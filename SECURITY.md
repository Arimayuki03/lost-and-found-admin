# 安全政策（Security Policy）

## 支持的版本

本项目目前处于初始开源阶段，安全修复仅针对最新 `master` 分支。

| 版本 | 支持 |
|------|------|
| master（最新） | ✅ |

## 报告漏洞

**请不要通过公开 Issue 报告安全漏洞。**

如发现安全漏洞，请私下联系仓库维护者（通过 GitHub 私信或仓库主页提供的联系方式），并在报告中尽量包含：

- 漏洞类型与影响的页面 / 模块；
- 复现步骤或概念验证（PoC）；
- 影响评估。

维护者会在确认后尽快跟进修复，并视情况发布补丁版本与致谢。

## 安全基线说明

- 本项目为纯前端管理端，认证依赖后端签发的 JWT（access_token / refresh_token），token 存储于 `localStorage`。部署时请确保整站 HTTPS，并自行评估 token 存储策略是否符合你的安全要求。
- 所有接口请求经 `src/api/index.js` 统一封装，请勿绕过拦截器直接发起请求。
- 生产部署请勿将管理端代理直接指向公网未鉴权后端；`vite.config.js` 中的 `localhost:5000` 代理仅用于本地开发。

## 已知范围外事项

- 后端服务（`lost_and_found`）的安全问题请到对应仓库反馈。
- 由部署环境（服务器、反向代理、CDN）引入的安全问题不在本仓库处理范围内。
